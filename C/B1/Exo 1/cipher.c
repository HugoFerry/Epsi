#include <stdlib.h>
#include <stdio.h>

int main() {
   char s[1024];

    int dice = rand() % 6 + 1;

    // Vous pouvez aussi effectuer votre traitement ici après avoir lu toutes les données 
    return 0;
}